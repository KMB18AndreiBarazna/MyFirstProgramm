import React from 'react';
import preloader from '../../../images/pre_loader4.gif'

let Preloader=(props)=>{
    return <div>
                <img src={preloader}/>
    </div>
};

export default Preloader;